#include "6dof_orbit_sim/elements.hpp"

// Definitions that don't live in the header go here.
// Currently the struct methods are inline; this file is a placeholder
// for any future non-inline element utilities (e.g. mean -> osculating,
// TLE parsing, element set I/O).

namespace orb {
} // namespace orb